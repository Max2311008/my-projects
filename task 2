#include <iostream>
#include <Windows.h>
int main() {
	SetConsoleOutputCP(CP_UTF8);
	SetConsoleCP(CP_UTF8);
	// 2, 4, 5 строки обрабатывают кириллицу
	int a; // Создаём переменную
	std::cout << "Введите катет a: ";
	std::cin >> a;
	//Вводим переменную a с помощью клавиатуры
	double b, c; // Создаём переменные 
	std::cout << "Введите катет b: ";
	std::cin >> b;
	if (a < 0 || b < 0) {
		std::cout << "ОШИБКА, введите положительные числа"; // Условие что все числа дожны быть положительными
	}
	else {
		//Вводим переменную b с помощью клавиатуры
		c = sqrt((a * a + b * b));// Вычисляем гипотенузу
		std::cout << "Гипотенуза = " << c << std::endl;// Выводим результат
		return 0;
	}
}
